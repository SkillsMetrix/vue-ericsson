
import { Directive } from 'vue'

const appColor: Directive = {
    mounted(el: HTMLElement, binding) {
        el.style.color = binding.value || 'black'
    },
    updated(el: HTMLElement, binding) {
        el.style.color = binding.value || 'black'
    }
}
export default appColor
---
<template>
 <p v-color="'red'">Welcome to directive</p>  
 <p v-color="textColor">Welcome to directive</p>  
 <button @click="textColor='green'">change</button>

</template>

<script setup lang="ts">
import {ref} from 'vue'
const textColor=ref('blue')
</script>

<style>
#app {
  font-family: Avenir, Helvetica, Arial, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-align: center;
  color: #2c3e50;
  margin-top: 60px;
}
</style>


------------

import { createApp } from 'vue'
import App from './App.vue'

import appColor from './directives/colordir'
createApp(App).directive("color",appColor).mount('#app')
